apiVersion: config.porch.kpt.dev/v1alpha1
kind: Repository
metadata:
  name: xxx
  namespace: default
  labels:
    kpt.dev/repository-content: external-blueprints
spec:
  content: Package
  deployment: true
  git:
    branch: main
    directory: /
    repo: http://gitea-http.gitea.svc.cluster.local:3000/nephio/xxx.git
    secretRef:
      name: xxx-access-token-porch
  type: git
